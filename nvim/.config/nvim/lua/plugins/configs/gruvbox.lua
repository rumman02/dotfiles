local gruvbox = require("gruvbox")

gruvbox.setup({
	contrast = "",
	overrides = {
		SignColumn = { bg = "none" },
		FlashLabel = { fg = "#fff100" },
	},
	dim_inactive = false,
	transparent_mode = false,
})

